// keep count a 5 
ComponentDidMount(){
counst count = localStorage.getItem("count")
if(count !==null) {
this.setState ({
count : parseInt(count)
}
}
}
// like method alert your change are not saved
Window.addEventListener("", () => {
localStorage.setItems("count", this.state.count)

})


